import { useAtom } from 'jotai';
import styled from 'styled-components';

function ItemDetailedInfo({ item }) {
    return <DetailedBox></DetailedBox>;
}

export default ItemDetailedInfo;

const DetailedBox = styled.section``;
